# Set these in your shell before starting Uvicorn; this file is documentation.
# Relative paths resolve from backend/. Production must use a persistent volume.
SQLITE_PATH=data/typeform.sqlite3
FRONTEND_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
